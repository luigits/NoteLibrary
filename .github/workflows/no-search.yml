name: build no search
on:

  push:
#    branches: ['master']
  workflow_dispatch:
  # 定时的优先级太低，改用定量 push
permissions:
  id-token: write
  pages: write

env:
  INSTANCE: 'Writerside/luigits-docs'
  DOCKER_VERSION: 'latest'
  IS_GROUP: 'true'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-24.04
    outputs:
      artifact: ${{ steps.define-ids.outputs.artifact }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
      
      - name: Define instance id and artifacts
        id: define-ids
        run: |
          INSTANCE=${INSTANCE#*/}
          INSTANCE_ID_UPPER=$(echo "$INSTANCE" | tr '[:lower:]' '[:upper:]')
          ARTIFACT="webHelp${INSTANCE_ID_UPPER}2-all.zip"
          
          # Print the values
          echo "INSTANCE_ID_UPPER: $INSTANCE_ID_UPPER"
          echo "ARTIFACT: $ARTIFACT"
          
          # Set the environment variables and outputs
          echo "INSTANCE_ID_UPPER=$INSTANCE_ID_UPPER" >> $GITHUB_ENV
          echo "ARTIFACT=$ARTIFACT" >> $GITHUB_ENV
          echo "artifact=$ARTIFACT" >> $GITHUB_OUTPUT
      
      - name: Build docs using Writerside Docker builder
        uses: JetBrains/writerside-github-action@v4
        with:
          instance: ${{env.INSTANCE}}
          docker-version: ${{ env.DOCKER_VERSION }}
          is-group: ${{ env.IS_GROUP }}
          artifact: ${{ steps.define-ids.outputs.artifact }}
      
      - name: Save artifact with build results
        uses: actions/upload-artifact@v7
        with:
          name: docs
          path: |
            artifacts/${{ steps.define-ids.outputs.artifact }}
            artifacts/report.json
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-24.04
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4  # ⚠️ 官方最新稳定版为 v4，v8 不存在
        with:
          name: docs
          path: artifacts
  
      - name: Check & Validate ZIP
        run: |
          echo "=== 目录内容 ==="
          ls -alh artifacts/
          echo "=== 文件真实类型 ==="
          file artifacts/*.zip
  
      - name: Unzip artifact
        run: |
          # 使用通配符替代变量，避免路径拼接错误
          unzip -qq "artifacts/${{ needs.build.outputs.artifact }}" -d dir
          echo "=== 解压结果 ==="
          ls -R dir/ | head -20
  
      - name: Setup Pages
        uses: actions/configure-pages@v5
        
      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dir  # 必须指向已解压的静态文件根目录
        
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
